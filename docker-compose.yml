services:
  # --- Tuần 1: nền tảng ---
  postgres:
    image: postgres:16
    container_name: jobmarket_postgres
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  crawler:
    build: ./crawler
    container_name: jobmarket_crawler
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy

  # --- Tuần 2: bật Airflow (bỏ comment khi tới lúc) ---
  # airflow-init:
  #   image: apache/airflow:2.9.0
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   env_file: .env
  #   entrypoint: /bin/bash
  #   command: -c "airflow db init && airflow users create --username admin --password admin --firstname A --lastname D --role Admin --email admin@example.com"
  #
  # airflow-webserver:
  #   image: apache/airflow:2.9.0
  #   container_name: jobmarket_airflow_web
  #   depends_on:
  #     - airflow-init
  #   env_file: .env
  #   volumes:
  #     - ./dags:/opt/airflow/dags
  #   ports:
  #     - "8080:8080"
  #   command: webserver
  #
  # airflow-scheduler:
  #   image: apache/airflow:2.9.0
  #   container_name: jobmarket_airflow_scheduler
  #   depends_on:
  #     - airflow-init
  #   env_file: .env
  #   volumes:
  #     - ./dags:/opt/airflow/dags
  #   command: scheduler

  # --- Tuần 4: bật Grafana (bỏ comment khi tới lúc) ---
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: jobmarket_grafana
  #   restart: unless-stopped
  #   env_file: .env
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   depends_on:
  #     - postgres

volumes:
  pgdata:
  # grafana-data:
